<odoo>
  <!-- REPLACE the old inherited template with this non-inheriting page -->
  <template id="ai_chat_page" name="AI Chat Page">
    <t t-call="website.layout">
      <t t-set="page_title">AI Chat</t>

      <section id="ai-chat-container" class="container my-5">
        <h1>AI Chat</h1>

        <!-- Standalone mount for /ai-chat -->
        <div class="ai-chat-min__panel" id="ai-chat-standalone" aria-live="polite"></div>

        <div class="text-muted mt-3">
          <!-- keep this simple so it never errors -->
          <span>No data is stored. Content may be sent to external AI.</span>
          <t t-if="privacy_url">
            — <a t-att-href="privacy_url" target="_blank" rel="noopener">Privacy</a>
          </t>
        </div>

        <!-- i18n bridge for JS only (not used by QWeb) -->
        <div id="ai-chat-i18n"
             data-title="AI Chat"
             data-send="Send"
             data-placeholder="Type your question…"
             data-close="Close"/>
      </section>
    </t>
  </template>
</odoo>
